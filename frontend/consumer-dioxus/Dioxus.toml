[application]
title = "Hmziq.rs Blog"

[web.app]

# HTML title tag content (overridden by application.title)
# title = "consumer-dioxus"

# include `assets` in web platform
[web.resource]

# Additional CSS style files
style = []

# Additional JavaScript files
script = []

[web.resource.dev]

# Javascript code file
# serve: [dev-server] only
script = []

[web.watcher]
reload_html = true

[web.ssg]
# Enable static site generation
enabled = true

# Use incremental SSG (only rebuild changed routes)
incremental = true
